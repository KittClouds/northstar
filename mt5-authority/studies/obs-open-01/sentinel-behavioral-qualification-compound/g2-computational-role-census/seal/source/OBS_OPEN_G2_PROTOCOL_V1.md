# G2 - Computational Role Census

Parent authority: `OBS_OPEN_G1_SENTINEL_SEMANTIC_KERNEL_V1`

Parent root:
`65cbe177fd5dc510ab9dc19e203a912a107ab8ac740b55a64926a1164edceafd`

## Governing question

What computational job does each distinction in the extracted G1 kernel
perform?

## Governing law

**CLASSIFY USE, NOT IMPORTANCE.**

G2 is a census, not a redesign. Roles are orthogonal and multi-role quantities
remain multi-role. G2 may record origin, carry status, guard use, arithmetic
form, identity/time behavior, context use, and emission use. It may not call a
field redundant, removable, minimal, necessary, sufficient, important, or
unimportant.

The candidate product `Q x R_guard x R_arithmetic x C x O x P` is treated as a
set of use axes, not a forced disjoint partition. `MULTI_ROLE_COLLISION` is an
earned descriptive result whenever a distinction crosses those axes.

## Required products

- an exhaustive field-role census;
- a causal carry-status ledger;
- a typed transition dependency graph;
- a multi-role collision registry;
- a role-coverage and graph-integrity receipt;
- explicit propagation of `GrammarStateAndEvent = NOT_EVALUABLE`.

Dependency edges are typed at minimum as `READS`, `GUARDS`, `UPDATES`,
`DERIVES`, `EMITS`, `COPIES`, `RESETS`, `INCREMENTS`, and `COMPARES`.

G2 consumes only the sealed G1 kernel, schemas, and dependency ledger. It does
not replay D_A and reads no D_B, D_C, or D_D data or outcomes.

## Nonclaims

G2 earns no removability, minimal-state, necessary-memory, reachability,
behavioral-equivalence, formal-machine-family, prediction, mechanism,
economic, or trading authority. G3 receives the anatomy map; it does not
receive a pre-decided ontology.

