# OBS-OPEN-03B-P2 — Inference Authority Freeze and Dependence Qualification

Authority class: scientific inference-protocol repair and qualification only.

This protocol preserves the sealed range-representation tournament, target,
score, 2% frozen-population materiality floor, D_B support gates, multiplicity
family, probe family, and all D_B/D_C firewalls.

The formal parameter is average session-population representation competence:

`mu_R = E[d_s^R]`, where `d_s^R = B_s(M0) - B_s(MR)`.

The formal hypotheses are `H0: mu_R <= 0` against `H1: mu_R > 0`. This is not
conditional sequential forecast dominance and is not design-based inference.

D_A dependence qualification uses four expanding-prefix, fixed-model validation
blocks. No covariance pair crosses a model-state boundary. The concatenated OOF
sequence is not treated as one stationary score process.

Any selected stochastic procedure must declare its stationarity, dependence,
moment, finite-sample/asymptotic, nuisance, support, and failure authorities.
D_A diagnostics can expose incompatibility; they cannot prove the unknown D_B
law. D_B and D_C remain unopened throughout this protocol gate.
