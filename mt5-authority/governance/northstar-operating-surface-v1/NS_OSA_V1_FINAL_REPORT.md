# NS-OSA-V1 Final Report — Northstar Operating Surface V1

## Executive result

`NORTHSTAR_OPERATING_SURFACE_V1` is sealed as a governance-only, deny-by-default operating substrate. The bounded O0–O7 campaign repaired the scheduler-to-execution lifecycle, introduced exact atomic capabilities, centralized authoritative insertion, derived consumability from immutable evidence, inventoried the tracked laboratory without opening scientific payloads, compiled every material finding into regressions, dry-projected G9–G12, and passed clean/adversarial qualification.

No scientific question was executed. No finite-box value was selected. No token, pair, fiber, comparison, certificate, G8 verdict, explorer result, population datum, real 04A history, target, or outcome was opened or created.

The historical `FC01-FQB-G1_V1` and `FC01-FQB-C1_V1` results remain immutable, unauthorized, and nonconsumable. The surface enables prospective lawful recovery; it does not rehabilitate either fossil.

## O0 — Bootstrap and audit charter

O0 bound `NS-OSA-V1` to `NS-GOV-ROOT_V1` as a constitutive governance operation. It created a restricted audit authority, froze FC01/scientific advancement, bounded the program to O0–O7, and denied retroactive authorization and protected/scientific access.

Result: `BOOTSTRAP_AND_AUDIT_CHARTER_SEALED`.

## O1 — Execution lifecycle repair

O1 implemented and qualified:

- Typed lifecycle: `DECLARED → ELIGIBLE → SELECTED → EXECUTION_AUTHORIZED → EXECUTING → EXECUTED → RESULT_SEALED → CONSUMABILITY_ESTABLISHED → DAG_RECOMPUTED`.
- Exact execution capabilities bound to scheduler receipt, gate ID/spec, parents, inputs, and authority roots.
- Atomic compare-and-seal consumption; exactly one racing execution can win.
- Revocation, replay, sibling-gate, changed-input, and changed-authority denial.
- One deny-by-default authority kernel and authoritative-insertion receipt.
- Derived consumability, including transitive blocked-ancestor collapse and cycle fail-closure.
- Separate chronology and topology validation.
- Explicit data-capability allowlists.
- Canonical JSON bytes with sorted map keys, preserved sequence order, and forbidden floating-point authority values.
- Read-only mmap hashing and compact typed roots/registries.

Implementation incidents fixed before qualification:

1. Small-vector serialization was initially absent; the engine did not qualify until exact serialization support was enabled.
2. Regression types initially prevented compilation; exact equality and race-join handling were repaired before evidence was accepted.
3. Clippy rejected a stale `map_or` form; the code was updated and `-D warnings` passed.

## O2 — Retrospective inventory and chronology

The O2 cut inventoried 3,802 tracked `mt5-authority` artifacts from Git index metadata. It recorded path, Git object ID, mode, stage, and coarse artifact class without opening payload content. The registry is split into pages of at most 500 records. Later O3–O7 artifacts are bound by the final seal rather than retroactively inserted into the O2 snapshot.

The operation registry contains 20 closed verbs. Unknown `(verb, artifact-class)` pairs deny. The chronology ledger preserves the G1/C1 authority failures and separates temporal validity from DAG validity.

## O3 — Material audit findings

Eight material failure classes were confirmed and dispositioned:

1. `OSA-FINDING-0001`: scheduler selection existed without execution authority.
2. `OSA-FINDING-0002`: authoritative insertion lacked one mandatory reference monitor.
3. `OSA-FINDING-0003`: consumability could be represented as hand-maintained state.
4. `OSA-FINDING-0004`: zero-access counters were evidence but not enforcement capabilities.
5. `OSA-FINDING-0005`: runner/validator canonicalization could diverge.
6. `OSA-FINDING-0006`: chronology was not uniformly typed as an authority precondition.
7. `OSA-FINDING-0007`: ambient execution inputs were not uniformly bound.
8. `OSA-FINDING-0008`: unbounded operating-surface refinement could starve science.

No style, naming, or merely conceptual observation was promoted into a material finding.

## O4 — Repairs and permanent regressions

All eight findings have positive and negative regression witnesses. The Rust suite contains 16 tests covering lifecycle omissions, exact capability binding, sibling/recovery aliasing, changed authority, atomic races, revocation, blocked ancestry, cycles, unauthorized insertion, protected data, canonical ordering, floating-point rejection, chronology, and mmap identity.

Historical defects were repaired prospectively. No sealed historical receipt was rewritten and no unauthorized result gained authority through resemblance.

## O5 — G9–G12 forward projection

G9, G10, G11, and G12 each traversed the generic dry lifecycle using synthetic/empty identifiers and `UNKNOWN_NOT_EXECUTED` scientific payloads. The projection covered authority binding, eligibility, selection, capability issuance/consumption, execution, sealing, typed results, derived consumability, DAG recomputation, failure, and recovery.

No missing generic operating primitive remained. This proves operating-protocol reachability only; it does not answer any G9–G12 scientific question.

## O6 — Clean rebuild and adversarial qualification

- Two isolated release builds on `D:` passed all 16 tests.
- A third same-path clean rebuild passed all 16 tests.
- Clippy passed with warnings denied.
- The atomic race test passed 100 repeated 32-thread races with exactly one winner each.
- Audit artifacts regenerated byte-identically.
- Static source scan found no system-clock, environment-variable, working-directory, filesystem-order, locale, timezone, or randomness dependency in authoritative engine code.
- Python qualification sources parsed cleanly.
- Every maintained file is at most 500 lines.

The PE test executables were not byte-identical between clean builds. This is recorded, not hidden. Binary identity is not claimed or used as authority. Semantic/regression replay and canonical authority-artifact identity passed. Source identity, build identity, and semantic identity remain separate.

One discarded O6 launch accidentally used Cargo's default target directory and introduced lock contention. It was stopped, cleaned with `cargo clean`, and excluded from all evidence. O6 was rerun from fresh isolated D: targets.

## O7 — Seal and anti-starvation law

All 17 closure conjuncts passed. Bootstrap mode is permanently closed. Any later operating-surface change must be a named descendant and must provide a material failure class, reproducer, affected operation, explanation of why V1 cannot represent it, and impact cone. Finer distinctions, naming, style, or aesthetic preferences cannot reopen pre-G9 infrastructure.

## Sol — positive authority report

The positive chain is explicit: `NS-GOV-ROOT_V1 → NS-OSA-V1 O0 → O1 operating contracts → O2/O3 evidence → O4 regressions → O5 projection → O6 replay → O7 seal`. Every newly consumable operating artifact is prospective. No authority flows from the unauthorized G1/C1 fossils.

## Kammi — adversarial report

The surface rejects missing, reused, revoked, sibling, descendant-aliased, input-rebound, and authority-rebound execution capabilities. It rejects unknown operation pairs, unauthorized registry insertion, blocked actors/ancestors, cycles, protected data without capability, bad chronology, semantic array sorting, and floating-point authority. Atomic consumption was race-tested rather than asserted.

## Kage — chain-of-custody closure

Kage's conjunction is exact: lifecycle, authority registry, operation registry, chronology, DAG, scope, failure/recovery, data firewall, input closure, canonicalization, runner/validator parity, deterministic semantic rebuild, regression suite, G9–G12 projection, derived consumability, atomic capability consumption, and one reference monitor all pass. Any failed conjunct would have blocked the seal.

## Uatu — operational disposition

The operating-system invention phase is closed. FC01 recovery may now be materialized as new candidate roots, followed by DAG recomputation, scheduler selection, exact single-use capability issuance, execution, result sealing, consumability derivation, and another DAG recomputation. The old G1/C1 selections and outputs cannot authorize their recovery descendants.

## Permanent restrictions and nonclaims

- Scientific authority: none.
- Population authority: none.
- Real 04A applicability: not established.
- G8 verifier authority: none.
- Finite-box values/instance authority: none.
- Historical binary/result rehabilitation: none.
- Cross-build PE byte identity: not claimed.
- G9–G12 scientific answers: unknown.

The ship is sanitized at the operating layer. The next work is lawful recovery of the blocked FC01 frontier under this sealed substrate, followed by the finite-box/token prerequisites and the return to G9 science.
