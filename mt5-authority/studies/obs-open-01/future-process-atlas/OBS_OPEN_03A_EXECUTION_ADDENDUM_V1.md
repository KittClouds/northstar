# OBS-OPEN-03A execution addendum

Parent protocol root: `3d1da3657154d5a4e11eca99cab021c71c9c8a5d6d4458469133f3d2b8e3a402`.

This addendum disciplines execution without amending the frozen scientific registry.

1. Materialize every admitted anchor, outcome, horizon, threshold, scale, and typed evaluability cell. Omission cannot mean uninteresting.
2. Every distribution declares its sampling unit. Preserve `ANCHOR_WEIGHTED` and `SESSION_WEIGHTED` views separately. Never transform one silently into the other.
3. Report anchors per session, eligible anchors per session, and contribution weights.
4. Use descriptive vocabulary only. No formal inference, promotion, information, prediction, mechanism, economic, or trading claim is authorized.
5. Every aggregate remains traceable to contributing sessions, anchors, knowledge time, outcome time, censoring, source-path status, normalization, and parent registry.
6. Preserve raw and normalized quantities as separately identified sibling views where normalization is admitted.
7. Any visualization is a deterministic projection of authoritative atlas artifacts. Question-generation notes, if later added, carry `HYPOTHESIS_GENERATION_ONLY` authority.
8. Apply `OUTCOME_REGISTRY_V1` only to `D_A`. Compute and inspect zero D_B outcomes. Decode and read zero D_C observations.
9. Construct no 03B protocol in this execution. Seal the descriptive atlas and stop.

Northstar doctrine established here:

> Every descriptive distribution declares its sampling unit.
