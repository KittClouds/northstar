# FC01A-A2 Final Partner Report

## Shared outcome

The exact G1-R executable instance was bound and invoked successfully. Applied `StepResult` objects transported into the frozen A1 ABI and were accepted by the unchanged A1 projector.

The overall A2 question is `NOT_EVALUABLE` because two G1 rejection returns do not contain the state, emission, and knowledge-time primitives required by the frozen ABI. The transport failed closed and did not manufacture them.

```text
RESULT = NOT_EVALUABLE
DISPOSITION = HOLD
FC01A = HOLD
FC01B+ = FROZEN
POPULATION = UNOPENED
```

## To Sol — exact descendant binding

One and only one G1-R materialization was selected:

```text
G1-R root:
21E310BA2B7E95ABFF2AA7B0F8165A4D44F2AF37FC7AB50BEF35DE76BF3FE2FB

executable SHA-256:
E1FD57AE1FEC58684D98A61AAC9F972821900291B2A44E9C211BECFDC8912AEA
```

The executable was invoked through its qualified `run` lifecycle. Its capture root matched the executable's own semantic envelope root. The alternate G1-R build was not mixed into A2; it remains independent qualification evidence only.

The ancestry remains explicit:

```text
sealed G1 semantic authority
    -> G1-R descendant authority
    -> selected BUILD-A executable instance
    -> StepResult capture
```

## To Sol — mechanical transport

For five applied `StepResult` records, transport used only:

```text
STEPRESULT.state
STEPRESULT.emissions.ordered
authorized G1 context
authorized observation provenance
G1 return-variant status
G1/G1-R lineage provenance
```

No semantic, lifecycle, genealogy, rejection, timing, reachability, fiber, or token logic was added. The A1 projector source hash remained unchanged.

## To Kammi — transport attack

The transport attack surface produced one decisive boundary witness.

For these G1 rejection cases:

```text
sequence_gap_fails_closed
unqualified_input_authority_rejected
```

G1 provided only a rejection status. It did not provide:

```text
state_projection
ordered_emissions
knowledge_time_projection
```

Those fields are mandatory in the frozen A1 ABI. A2 therefore emitted:

```text
G1_REQUIRED_PRIMITIVE_UNAVAILABLE
```

No placeholder state, empty emission list, inferred timestamp, or derived convenience formula was introduced.

Hash binding also blocked executable substitution, G1-R lineage substitution, silent build mixing, and projector mutation.

## To Kammi — replay

The same selected executable was run twice under the declared A2 environment. Replay A and Replay B were byte-identical:

```text
8035898AB39026C8536640DC25A3BE375114DA74617872465CD5F1693BAA28D3
```

This is semantic/transport replay authority for the declared synthetic scope. It is not binary reproducibility and does not extend scope to real histories.

## To Kage — closure

The conjunction is:

```text
exact G1-R instance binding       = PASS
G1-R invocation/capture           = PASS
applied StepResult transport      = PASS
unchanged A1 projector            = PASS
replay                            = PASS
rejected-return transport         = NOT_EVALUABLE
overall A2                        = NOT_EVALUABLE
```

The failure is located at the frozen ABI boundary, not in G1-R and not in A1 projection-only qualification.

This does not close FC-01A. It records that the descendant callable works, but the frozen doorway cannot represent every declared G1 return variant without an authorized ABI change.

## Preserved fossils

```text
G1-M = CLOSED / NOT_EVALUABLE
G1-R = CLOSED / QUALIFIED_WITH_RESTRICTIONS
A1_ATTEMPT_1 = IMMUTABLE / PROJECTION_ONLY_QUALIFIED
HISTORICAL_G1_BINARY_IDENTITY = NOT_CLAIMED
```

No historical result was rewritten. No projector was changed. No G1-R source was changed.

## Access audit

```text
real 04A history reads       = 0
population reads             = 0
target reads                 = 0
outcome reads                = 0
pair reads                   = 0
question tokens              = 0
arm executions               = 0
D_B / D_C / D_D reads        = 0
```

## Next legal state

No FC-01B work begins. A new decision is required if the project wants to amend the ABI or define a separate rejection transport. Such work would be a new lineage; it is not an A2 repair.
