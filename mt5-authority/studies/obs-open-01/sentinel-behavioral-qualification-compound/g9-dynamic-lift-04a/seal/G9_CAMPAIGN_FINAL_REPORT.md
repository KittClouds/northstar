# G9 Dynamic Lift of 04A — Unified Final Campaign Report

## Executive result

G9 is qualified with restrictions for the actual 04A compression `CURRENT_SENTINEL_STATE_V1 -> REDUCED_SENTINEL_GEOMETRY_V1` over the declared D_A scope.

The map-level universal preservation claim is falsified. Exact G1 replay produced 57,500 constructively reachable prefix states in 154 D_A sessions. The sealed 04A projection produced 46,807 observed reduced fibers, including 6,369 collision fibers and 138,962 same-fiber pairs. All 138,962 pairs satisfied the recovered G6 context fiber and reachability conditions. Under the frozen 1,024-pair primary/characterization budget, all 1,024 tested pairs fractured immediately at epsilon on protected transition ordinal. Those witnesses falsified universal homogeneity in 566 fibers. The remaining 5,803 collision fibers are explicitly `UNSEARCHED`, not preserved.

Primary science root:

```text
3844cc9dafe1f54aa54fb501381f16faf3b5c67958bdafc2e54d50325b55b5db
```

Complete run root:

```text
a0ab7992bb0bce185185d85e9a4b1186c10b4847059eda220995c3cbbee73478
```

## Sol — semantic sovereignty and positive construction

- G1 alone constructed lifecycle state, candidate genealogy, timing, emissions, and rejection semantics.
- G9 called the exact sealed integer G1 kernel and its public `project_state_to_04a` boundary.
- Same-fiber keys were encoded only after the sealed `ReducedState::from(&CommittedState)` projection.
- G9 added no lifecycle, genealogy, rejection, or temporal reconstruction rule.
- G5 relative continuations and G6 affine comparison rules remained inherited authority. They were frozen in the chamber, although every searched pair closed at epsilon before a nonempty continuation was needed.
- The historical G8 witness laboratory was not relabeled as real-history authority. Its certificate source contract is synthetic-only. G9 therefore used a newly named, restricted `G8_REAL_HISTORY_EXACT_VERIFIER_DESCENDANT_V1` that recomputed claims from G1/G4/G5/G6 and possessed no explorer authority.
- Six other 04A relation surfaces remain `NOT_EVALUABLE`; private 04A helper logic was not copied to manufacture an exact path.

## Kammi — adversarial findings and repairs

### Preauthority discovery incident

A broad repository search exposed sealed 04A result metadata before the G9 D_A capability was sealed. The affected surfaces are recorded in `PREAUTHORITY_DISCOVERY_ACCESS_INCIDENT_V1.json`. Those observations were quarantined, created no scientific result, and were not used to select the map, pair order, pair budget, token box, or stopping rule. The authoritative execution reread raw D_A only after consuming the exact single-use capability.

### Apparatus attempts

1. The first preflight build accidentally used a deep C: Cargo target and failed with Windows linker `LNK1104`. No science capability was consumed. All subsequent builds used isolated D: targets.
2. Build A completed the science but remained non-consumable because its evidence surface serialized `EPSILON_ONLY` incorrectly, omitted a materialized fiber-status ledger, and named verifier receipts without materializing their payloads. The science/search logic was unchanged; the attempt remains a fossil in the repair ledger.
3. Builds B and C were byte-identical and established replay evidence, but review found that the final package still needed the complete upstream access audit, an explicit 04A contract-audit finding, and source/constitution custody. They remain recorded as qualification evidence, not the final package.
4. Builds D and E independently rebuilt and reran the frozen chamber. Their 33 recursive files were byte-identical. Every inner manifest member and every source snapshot matched. Build D supplied the promoted instance; Build E supplied independent replay evidence.
5. An inline cleanup command was rejected by sandbox policy before mutation. A checked PowerShell cleanup script then removed exactly 11 generated build/target directories after validating every resolved path. The seal was reverified afterward.

### Contract blind spot found

The historical 04A edge declares lost degrees `CANDIDATE_IDS`, `BIRTH_TIMES`, and `CAUSAL_AGES`. The actual sealed source and target specifications also show that `bar_index` and `knowledge_time` disappear from the reduced representation. G9 therefore seals `04A_DECLARED_LOST_DEGREES_UNDERENUMERATED`. It does not rewrite 04A. The actual map projection remains unchanged, and the first 1,024 frozen fractures land precisely on the protected transition ordinal omitted by that historical list.

## Kage — custody, access, and quantifiers

### Authority lifecycle

The G9 constitution, frozen search precommit, exact D_A capability, access-incident receipt, and repair ledger were hashed before each final execution. Each build consumed a fresh single-use capability bound to its exact gate specification, authority roots, input roots, and execution identity.

### Access audit

```text
raw D_A SHA-256       = 125b768ad87ba578c5498deffe50462909a6a1be422953a0f7488f036aac9d5b
source prefix rows    = 633322
D_A sessions decoded  = 154
D_A bars replayed     = 57500
D_A path-gap sessions = 14
D_B reads             = 0
D_C reads             = 0
D_D reads             = 0
target reads          = 0
outcome reads         = 0
external optics preseal = 0
```

The 14 path-gap sessions contributed only retained causal prefixes. No continuation beyond a source gap entered evidence.

### Quantifier separation

- Pair: 1,024 exact `DISTINGUISHABLE_WITH_WITNESS` results.
- Fiber: 566 `UNIVERSAL_HOMOGENEITY_FALSIFIED`; 5,803 `UNSEARCHED` collision fibers.
- Map: `UNIVERSAL_BEHAVIORAL_PRESERVATION_FALSIFIED` for the executed state-to-reduced map only.
- No complete fiber partition is claimed.
- No complete map characterization is claimed.
- Search rank is not behavioral divergence depth.
- Bounded silence is not equality or zero.
- No global minimum witness or continuation-space density is claimed.

## Scientific anatomy

For every verified specimen in the frozen budget:

```text
f(a_i) = f(a_j)
u* = epsilon
O(a_i, epsilon) != O(a_j, epsilon)
tau_comp = 0
tau_obs = 0
first protected fracture = COB_TRANSITION_ORDINAL
```

The compression identifies reachable states that are already distinguishable on the frozen protected surface at the comparison boundary. This is an immediate fracture, not delayed re-emergence. It proves that universal behavioral preservation fails for this map; it does not establish how the unsearched fibers decompose.

## Restrictions and nonclaims

- Scope is D_A and the executed state-to-reduced map only.
- G4's `GrammarStateAndEvent` blind spot remains open.
- Other 04A maps remain `NOT_EVALUABLE` pending exact public projection and verifier descendants.
- Parallax sidecars are `NOT_EVALUABLE`; no external optic transport entered this lineage.
- G10 receives fracture evidence only. Necessity, minimal memory, and representation prescriptions are not claimed.
- No market meaning, prediction, economic authority, or trading authority is created.

## Final disposition

The laboratory is clean, the primary result is sealed, independent replay passes, and the qualified evidence can now enter the next scientific question under its stated restrictions. G9 has answered one actual 04A map decisively: reduced sentinel geometry forgets protected causal position, so its fibers are not behaviorally homogeneous even before the observer is advanced.

