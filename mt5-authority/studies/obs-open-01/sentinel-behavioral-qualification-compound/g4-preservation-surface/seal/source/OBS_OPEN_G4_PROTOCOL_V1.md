# G4 — Causal Observer Preservation Surface

Lifecycle begins `NOT_STARTED / OPEN / NONE / NONE`. Closure is earned only by the seal.

G4 freezes `A_COB = (O,S,T,C,B)`: named observables, scopes, temporal/ordering authority, semantic interpretation context, and declared blind spots. It answers only which distinctions are protected. Cross-history comparison is `DEFERRED` or `NOT_APPLICABLE` throughout.

G4 does not define equivalence, correspondence, jointly admissible continuations, joint presentability, candidate-ID matching, witnesses, simulation, bisimulation, quotients, removability, minimality, or optimization.

All 57 G2 elements receive a normative disposition distinct from the named-observable ontology. Exact semantic retention inside an execution never implies literal equality across histories. Candidate identity is retained as genealogy without defining label correspondence. Inter-transition and intra-transition order are independently protected.

G3's mixed, context-indexed reachability sandwich is inherited unchanged. Upper-envelope counterexamples require constructive witnesses; lower-corpus absence cannot prove unreachability. `P_EXEC_G3` is not a comparison-continuation universe.

Grammar remains `NOT_EVALUABLE`. No outcome population is read. The sealed surface is immutable for this descendant lineage; any change requires a new G4 root and reexecution of G5 and later gates.
