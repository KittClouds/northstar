# G5 — Admissible Continuation Grammar

Lifecycle begins `NOT_STARTED / OPEN / NONE / NONE`. G5 defines lawful future experiments; it does not compare protected outputs.

The primitive start is `(executable prefix, reached state, immutable context, reachability receipt)`. Unilateral grammars are prefix-indexed. Every grammar contains epsilon and is prefix closed. State sufficiency is not assumed.

Shared experimental meaning is represented by neutral tokens and a typed, prefix-sensitive coupling Lambda producing side-specific completed observations. Literal input identity is only a qualified special case. Stimulus coupling and observer correspondence are separate; this grammar requires no observer correspondence to determine source presentability.

Jointly presentable is not jointly applied. Source grammar invalidity, context invalidity, prefix invalidity, kernel application, and typed kernel rejection are separate layers. Rejection is protected behavior and does not become a pre-presentation exclusion.

G3 reachability remains mixed. The global result is therefore a sound, fully indexed all-finite envelope with constructive finite lower witnesses. Upper-envelope starts remain conditional and require constructive validation before later counterexample authority.

No equivalence, correspondence instance, distinguishing witness, simulation, bisimulation, quotient, removability, or minimization is defined. GrammarStateAndEvent remains an independent `NOT_EVALUABLE` blind spot. D_A/D_B/D_C/D_D outcomes are untouched.
