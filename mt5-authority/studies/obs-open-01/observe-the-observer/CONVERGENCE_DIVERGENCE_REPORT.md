# OBS-OPEN-OTO-G8.1–G8.5 — Two-Head Convergence/Divergence Report

**Campaign form:** one campaign, two heads

**Head S:** Sol — constructive optical/parallax architecture

**Head K:** Kammi — adversarial failure, steering, and contamination audit

**Foundation:** Observe-the-observer (OTO) static anatomy and authority-surface work

**Report status:** `SEALED_SYNTHESIS_NO_NEW_AUTHORITY`

**Population status:** unopened

**Scientific result:** none

**Economic/trading authority:** none

This report ends the blind separation of the two branches. It does not merge their artifacts, rewrite either lineage, select a question, bind a target or region, open a population, or authorize an arm. It records where the heads independently arrived at the same constitutional boundary, where they solve different problems, what each eliminated, what each bought, and which states may safely be collapsed for campaign navigation.

The governing interpretation is:

> Sol and Kammi are not competing studies. They are two heads of one campaign: Sol designs lawful ways to vary visibility; Kammi tests whether the resulting campaign can fool itself. OTO supplies the manufactured observer and the limits of what can presently be claimed about it.

---

## 1. Executive finding

The branches are directionally convergent but operationally complementary.

Sol asks:

> What lawful optical transformations, views, and question forms could expose distinctions without naming the thing being viewed?

Kammi asks:

> How could a campaign with those views, questions, and arms manufacture apparent separation, convergence, neutrality, or significance?

The shared result is not a scientific discovery. It is a narrowed constitutional state:

```text
ONE_CAMPAIGN_TWO_HEADS = TRUE
POPULATION_AUTHORITY   = NONE
TARGET_BINDING         = UNBOUND
REGION_BINDING         = UNBOUND
ARM_EXECUTION          = FORBIDDEN
G8_LINEAGE             = NOT_MATERIALIZED_IN_CURRENT_CHECKOUT
ARM_INDEPENDENCE       = NOT_CLAIMED
ONTOLOGY               = NONE
```

The safe collapse is therefore at the level of campaign prerequisites and nonclaims, not at the level of optical arms, attack surfaces, or scientific findings.

---

## 2. Authority lineage

### Common OTO ancestry

| Artifact | Root / status | Authority earned |
|---|---|---|
| OTO static census | `145fe2f99c75e1214f6cf38b75e83538f4cfef46c5f04b1edfa7ba87b0cd3c8d` | Static observer anatomy and instrumentation census only |
| OTO-A1 surface alignment | `6f5209a690d0a860bf9916741d5298275a7b18d1c7fe984ab2d5aefb962a0157` | Corrected mapping of implementation publication, protected observation, display, and capture |
| OTO-L adjacency atlas | `24003a167236071b27ddab86769023528e76185fe60825608dbf605cfe13a472` | Literature/formalism adjacency; no theorem transfer |
| OTO-RT transport audit | sealed, `TRANSPORT_NOT_QUALIFIED` | Dynamic branch halted; no 6106→6116 transport authority |

### Head S — Sol lineage

| Artifact | Root / status |
|---|---|
| Sol blind campaign | `f25ba4971834384a76b96eb090306773dfba304be8250ce1519d175deeeec35d` / `SEALED_WITH_DECLARED_RESTRICTIONS` |
| Sol Part 2 question factory | `d1b38643e473ff5a2026bd9f244d2727d9cba08d608007d6ebc24d0c16004052` / `QUESTION_FACTORY_QUALIFIED_PENDING_G8_BINDING` |
| Sol P2B envelope rebinding | `cb4066a452c7004f17547b05ccb83ed2211617ffadc1b36a995075ab3ec62394` / `G8_LINEAGE_REBIND_BLOCKED` |

### Head K — Kammi lineage

| Artifact | Root / status |
|---|---|
| Kammi blind campaign | `d1e78ba54e4e87cc5cbbab275b57aaf5d67f308b4b390d13ba93ecf98b876025` / `BLIND_TRIANGULATION_CAMPAIGN_SEALED_WITH_RESTRICTIONS` |
| Kammi P2 steering audit | `cd9ea746f3d1712b032ccf582cc1e54c1d8ebfa751e79f7ffe7f93d2a1c42405` / `PARALLAX_FAILURE_AUDIT_SEALED_WITH_RESTRICTIONS` |

Both heads independently record the same unavailable G8 parent condition. Neither synthesized a root from a naked hash, memory, or source inference.

---

## 3. OTO foundation: what the observer work bought

The OTO work is the common manufactured-object layer. It prevents the campaign from treating an implementation surface, a display, a runtime, and a causal observer as interchangeable.

### 3.1 Static anatomy earned

The static census identified, without market or outcome inspection:

```text
24 declared input nodes
12 ambient-coordinate nodes
22 latent-policy nodes
58 identified joints
12 render-only controls
2 unresolved joints
54 published buffers
```

The observer is not merely a time-range parameterization. It is a composition of:

```text
explicit inputs
ambient platform/history coordinates
hard-coded semantic policies
causal state architecture
legacy and causal publication surfaces
```

The independent dimension count remains `NOT_EVALUABLE`. The 58 joints are a census, not a 58-dimensional coordinate system.

### 3.2 Semantic seams preserved

OTO established or preserved the following distinctions:

```text
legacy repaint-compatible output      vs causal output
forming/provisional state              vs completed/committed state
current candidate                      vs retrospective terminal survivor
close-observed grammar                 vs high/low-observed sentinels
sentinel gap fail-closed               vs grammar gap resume
candidate identity and birth time      vs display values
implementation data publication       vs display rendering
G4 protected observation               vs instrument capture visibility
```

These are not interpretations of the unknown. They are seams in the manufactured observer and in the measurement apparatus.

### 3.3 Intervention and authority topology

OTO-I/OTO-C/OTO-A/OTO-A1 showed that a source-level edit is not the same thing as a lawful observer intervention:

```text
MUST_REWRITE_WITH             = 0
MUST_REPROVE_WITH             = 35
AUTOMATIC_DOWNSTREAM_EFFECT   = 149
```

The corrected A1 view classifies the coarse authority impact as:

```text
G0-earliest invalidation       = 15
G1-earliest invalidation       = 29
display-only / no G0–G8 debt   = 12
unknown                        = 2
```

The twelve display controls are not twelve scientific observer redesigns. A change in color, style, width, or visibility is not a G4 change unless the G4-protected observation surface is proven to change.

### 3.4 Dynamic boundary

OTO-RT did not qualify a runtime bridge:

```text
6106→6116 transport            = NOT_QUALIFIED
effective parameter binding    = NOT_EVALUABLE
completed-bar dynamic authority = NONE
live forming-bar authority     = NONE
general MT5 runtime equivalence = NOT_CLAIMED
Trading.com editor             = QUARANTINED
```

This is a boundary, not an observer failure. Static dissection is open. Dynamic parameter intervention, runtime sweeps, and runtime-derived observer claims remain blocked.

### 3.5 Literature boundary

OTO-L sealed 14 adjacency records:

```text
6 mechanism records
8 qualification records
9 partial interface matches
5 not proven
5 negative-adjacency findings
14 theorem transfers not attempted
authority gain = NONE
```

Literature supplied vocabulary and neighboring methods, not membership or theorem authority.

---

## 4. Head S — Sol: constructive optical/parallax architecture

Sol built the lawful glass around the observer.

### 4.1 What Sol constructed

Sol's campaign freezes:

```text
observer ≠ optic ≠ interpretation
optic loss/time/order/invertibility receipts
no optic feedback into the observer
resolution neutrality
no retrospective precision upgrade
neutral question factory
five-outcome epistemic delta: YES/NO/MIXED/UNKNOWN/NOT_EVALUABLE
non-ontological synthesis
cross-arm blindness
predeclared stopping rules
```

The six proposed optical arms are:

| Arm | Primitive | Status |
|---|---|---|
| `SOL-P1` | Clock/coordinate parallax | Protocol freeze only |
| `SOL-P2` | Granularity/resolution parallax | Protocol freeze only |
| `SOL-P3` | Partial-information parallax | Protocol freeze only |
| `SOL-P4` | Temporal deformation | Protocol freeze only |
| `SOL-P5` | Fixed multiscale optics | Protocol freeze only |
| `SOL-P6` | External microfilm / finer source | Blocked pending separate source |

Sol explicitly leaves all 15 pairwise arm relationships `UNKNOWN`. The arms are deliberately different instruments, not proven independent experiments.

### 4.2 What Sol eliminated

Sol removed or blocked:

```text
target ontology selection
target-region binding
result-conditioned question selection
adaptive scale hunting
preferred-clock assumptions
finer-is-better assumptions
forced common coordinate systems
latent-object construction
majority-vote synthesis
external-resolution smuggling
population contact
```

### 4.3 What Sol bought

Sol bought a forward-facing campaign architecture:

```text
lawful views
loss contracts
question-factory qualification
synthetic anti-funnel tests
negative controls
arm-level blindness
packet-level epistemic deltas
non-comparable / orthogonal synthesis states
```

The Sol Part 2 factory froze five candidate packets (`SOL-P1-Q1` through `SOL-P5-Q1`) that survive synthetic anti-funnel qualification. This is protocol competence only. It is not evidence about 04A.

P2B strengthened lineage discipline by rebinding through an external envelope only:

```text
parent packet mutation = FORBIDDEN
packet regeneration    = FORBIDDEN
packet hash unchanged  = PASS_BYTE_UNCHANGED
G8 binding             = NOT_EVALUABLE
```

---

## 5. Head K — Kammi: adversarial steering and contamination audit

Kammi built the campaign's immune system.

### 5.1 What Kammi attacked

Kammi P2 froze and exercised 16 attack surfaces before any population contact. The attack families include:

```text
ontology leakage
region leakage
fake parallax
shared-bug convergence
question multiplicity
translation laundering
latent-subject accumulation
narrative majority
result-opportunity asymmetry
resource steering
cross-arm information-flow side channels
shared trusted-computing-base dependence
formal-tool attraction
admission selection
metadata visibility
unknown instance-dependent steering
```

It also froze 12 metamorphic expectations and five synthetic control families.

### 5.2 What Kammi eliminated

Kammi blocked the following channels under the sealed contracts:

```text
fake parallax from representation relabeling
translation-chain laundering
latent-subject accumulation
narrative majority authority
question/arm multiplicity drift
result-conditioned protocol adaptation
cross-arm information-flow side channels
formal-tool selection by attractiveness
shared-bug convergence being treated as confirmation
```

It did not claim those channels are absent in every future instance. It typed five areas as `INSTANCE_DEPENDENT` and made a pre-touch instance audit mandatory.

### 5.3 What Kammi bought

Kammi bought a backward-facing red-team layer:

```text
pre-touch instance steering audit
admission-selection audit
resource/opportunity audit
shared-TCB topology
cross-arm information-flow audit
metamorphic controls
typed attack status vocabulary
repair nonapplication receipts
explicit unknown-channel preservation
```

Kammi's strongest negative result is deliberately scoped:

```text
NO_EXPLOIT_FOUND_UNDER_DECLARED_ATTACK_SURFACE
```

That is not campaign neutrality, unbiasedness, arm independence, complete parallax, or scientific validity.

---

## 6. Convergence: where both heads arrived together

| Shared proposition | Sol expression | Kammi expression | Collapsed campaign state |
|---|---|---|---|
| Do not name the unknown early | Null ontology; optic/interpretation split | Unbound target and region; vocabulary firewall | `ONTOLOGY_NONE`, `TARGET_UNBOUND`, `REGION_UNBOUND` |
| Protocol before population | Question factory and launch seal | First-touch admission and blind launch seal | `POPULATION_AUTHORITY_NONE` |
| Unknown is a valid outcome | `UNKNOWN` / `NOT_EVALUABLE` packet branches | Attack status and instance-dependent outcomes | `BLIND_SPOT_TYPED_NOT_ERASED` |
| No independence by design intent | Pairwise arm relationships remain `UNKNOWN` | No arm-independence score or majority authority | `ARM_INDEPENDENCE_NOT_CLAIMED` |
| No majority vote | No majority synthesis | Narrative majority explicitly blocked | `MAJORITY_AUTHORITY_NONE` |
| No forced common object | No latent object/master geometry | No latent subject/master state/common manifold | `NO_FORCED_ONTOLOGY` |
| No result-conditioned retuning | Result-independent question factory | Remediation and adaptation forbidden | `PROTOCOL_MUTATION_FORBIDDEN` |
| Synthetic tests are machinery tests | Anti-funnel and negative controls | Metamorphic and steering controls | `SYNTHETIC_ONLY_NO_POPULATION_EVIDENCE` |
| Lineage must be materialized | P2B refuses naked G8 hash | Kammi preserves unavailable G8 parent | `G8_LINEAGE_UNRESOLVED` |
| Higher resolution is not historical authority | P6 blocked | No source substitution | `EXTERNAL_SOURCE_AUTHORITY_REQUIRED` |
| The observer and its views differ | Optic does not alter observer | Arm/adapters cannot silently change lineage | `OBSERVER_OPTIC_SEPARATION` |
| Trading.com is not an authority source | No runtime/editor use | Editor access remains false/quarantined | `TRADING_EDITOR_QUARANTINED` |

These are legitimate collapse points because they describe the shared constitutional boundary, not a shared scientific answer.

---

## 7. Divergence: what must remain separate

The following differences are not defects. They are the two heads covering different failure modes.

| Dimension | Sol head | Kammi head | Treatment |
|---|---|---|---|
| Primary direction | Construct lawful views and questions | Attack the campaign for self-deception | Preserve both |
| Primitive unit | Optic / view / arm | Attack surface / steering channel | Do not map one-to-one |
| Main threat | Visibility loss, timing, resolution, partial information | Procedural adaptation, common mode, selection, TCB, side channels | Orthogonal ledgers |
| Arm architecture | Six optical arms P1–P6 | Five evidence paths A–E | Preserve native taxonomies |
| External source | P6 microfilm is separately blocked | Admission/source authority is audited as a channel | Do not collapse into one “data issue” |
| Question authority | Forward question factory | Backward audit of how questions can bias outcomes | Sequence them; do not substitute one for the other |
| Synthesis language | Agreement, disagreement, orthogonal, shared blind spot, common mode | Typed attack outcomes and consequence classes | Translate only through a future contract |
| Unknown type | Optical non-comparability / blind spot | Unknown steering or instance dependence | Same label is not same uncertainty |
| First-touch stop | G8 binding and population access review | Mandatory instance steering audit | Composite gate must require both |
| Observer relation | Optics preserve the creature while changing glass | Interventions may create a new observer lineage | OTO authority governs the boundary |
| Dynamic status | No runtime arm execution | No runtime probe or population execution | Shared result, different route to it |

Safe campaign language is therefore:

> Sol supplies the lawful ways to ask. Kammi supplies the tests that determine whether asking that way can be trusted.

---

## 8. What the combined campaign has eliminated

The two heads, together with OTO, have removed the following temptations from the current authority chain:

1. A preferred ontology for the unknown.
2. A target or region chosen because it would be interesting.
3. A population opened merely because a protocol looks complete.
4. A claim that different optics are independent because they have different names.
5. A claim that agreement by arm count is evidence.
6. A claim that failure to find an exploit means neutrality.
7. A claim that failure to find a witness means equivalence.
8. A claim that synthetic controls establish real-population validity.
9. A claim that finer timestamps create finer historical knowledge.
10. A claim that renderer changes alter causal-observer authority.
11. A claim that a source-level edit is an isolated scientific intervention.
12. A claim that the missing G8 root can be reconstructed from memory or hash alone.
13. Dynamic parameter sweeps on an unqualified runtime.
14. A new scientific object assembled from cross-arm outputs.
15. Economic, trading, mechanism, or universal observer interpretations.

---

## 9. What the combined campaign has bought

The campaign now owns a much narrower but much stronger toolkit:

### Observer substrate

```text
static observer anatomy
latent-policy census
ambient-coordinate census
intervention cones
authority-impact topology
surface alignment
runtime boundary
literature adjacency
```

### Constructive head

```text
qualified optic registry
loss/time/order/invertibility receipts
neutral question factory
six-arm parallax architecture
synthetic anti-funnel qualification
negative-control contract
non-ontological synthesis
```

### Adversarial head

```text
16-surface steering attack registry
12 frozen metamorphic expectations
synthetic controls
common-mode and shared-TCB topology
admission/resource/opportunity audits
cross-arm information-flow audit
mandatory instance audit before first touch
```

The bought object is not “a way to discover the unknown.” It is:

> a controlled, two-headed campaign apparatus that can expose its own blind spots before any protected population is opened.

---

## 10. Safe state collapse map

The following states may be collapsed into one campaign-level navigation state because both heads independently establish them:

```text
CAMPAIGN_POPULATION_AUTHORITY_NONE
    = no target values, histories, pairs, outcomes, or arm results read

CAMPAIGN_TARGET_UNBOUND
    = no scientific object or target region selected

CAMPAIGN_G8_LINEAGE_UNRESOLVED
    = no materialized parent G8 root; no substitute accepted

CAMPAIGN_ARM_EXECUTION_FORBIDDEN
    = protocol design and synthetic qualification only

CAMPAIGN_ARM_INDEPENDENCE_NOT_CLAIMED
    = pairwise relationships/common mode remain unresolved

CAMPAIGN_NO_AUTHORITY_GAIN
    = synthetic, static, literature, and red-team work have not earned population claims

CAMPAIGN_DYNAMIC_BRANCH_BLOCKED
    = OTO runtime transport and effective binding remain not evaluable

CAMPAIGN_EXTERNAL_SOURCE_BLOCKED
    = finer external measurement requires a separate source authority

CAMPAIGN_TRADING_EDITOR_QUARANTINED
    = no editor/runtime contamination accepted
```

These collapse states are for coordination only. They must not delete branch-native detail. In particular, `UNKNOWN`, `NOT_EVALUABLE`, `NOT_COMPARABLE`, `INSTANCE_DEPENDENT`, and `NO_EXPLOIT_FOUND_UNDER_DECLARED_ATTACK_SURFACE` remain distinct in their native artifacts.

---

## 11. Divergence preservation ledger

The following must remain separate in every future campaign receipt:

```text
SOL_OPTIC_ARM_ID                 ≠ KAMMI_ATTACK_SURFACE_ID
SOL_VIEW_LOSS                    ≠ KAMMI_STEERING_RISK
SOL_NOT_COMPARABLE               ≠ KAMMI_UNKNOWN_CHANNEL
SOL_P6_SOURCE_BLOCK              ≠ KAMMI_INSTANCE_ADMISSION_DEPENDENCY
OTO_RUNTIME_NOT_EVALUABLE        ≠ CAMPAIGN_NEUTRALITY_NOT_ESTABLISHED
DISPLAY_CHANGE                   ≠ CAUSAL_OBSERVER_CHANGE
SYNTHETIC_QUALIFICATION          ≠ POPULATION_VALIDITY
NO_EXPLOIT_FOUND                 ≠ NO_EXPLOIT_EXISTS
NO_WITNESS_FOUND                 ≠ EQUIVALENCE
```

The campaign may create a cross-head relation such as `ORTHOGONAL_FINDINGS` or `SHARED_BLIND_SPOT` only when a future translation certificate proves that the native outputs are comparable. No such cross-head scientific result exists in this report.

---

## 12. Recommended next move: one composite first-touch gate

Do not fan out into more broad branches. The next lawful step is a narrow campaign gate with two heads operating under one constitution.

### `OTO-CAMPAIGN-FIRST-TOUCH-AUTHORITY`

#### Head S obligations

```text
verify exact G8 materialization
bind through the external P2B envelope only
reverify frozen question packets byte-identically
preserve P6 blocked status
preserve optic/arm/synthesis/stopping/blindness hashes
```

#### Head K obligations

```text
run the concrete instance steering audit
verify admission metadata visibility
verify resource/opportunity neutrality
verify shared-TCB and side-channel boundaries
verify arm output isolation
verify no result-conditioned remediation
```

#### Joint obligations

```text
target and region remain explicitly bound or explicitly unbound
population access scope is frozen before first touch
OTO static authority is the only observer authority available
OTO dynamic authority remains NONE unless independently qualified
Sol and Kammi outputs remain blind until a declared synthesis unlock
P6 cannot hitchhike on G8 binding
all source/data counters are reverified at seal
```

The gate may issue only:

```text
FIRST_TOUCH_AUTHORIZED
FIRST_TOUCH_BLOCKED
NOT_EVALUABLE
```

It must not itself issue a scientific result. If it passes, a later mission may authorize admission-only contact. If it fails, the campaign remains sealed and the failure is typed.

---

## 13. Current access and authority audit

Across OTO, Sol, and Kammi artifacts reviewed here:

```text
04A source/content reads during campaign design     = 0
04A real history/pair reads                          = 0
D_B / D_C / D_D reads                                = 0
target/outcome reads                                 = 0
Sol arm executions                                   = 0
Kammi arm executions                                 = 0
runtime probes                                       = 0
result-conditioned retuning                          = 0
Trading.com editor access                            = 0 / QUARANTINED
Moxie access                                         = FORBIDDEN
```

The OTO static census is an authorized metrology artifact, not a population outcome. The campaign remains pre-interrogation.

---

## 14. Final convergence/divergence decision

```text
CAMPAIGN_FORM:
    ONE_CAMPAIGN_TWO_HEADS

HEAD_S:
    CONSTRUCTIVE_OPTICAL_PARALLAX

HEAD_K:
    ADVERSARIAL_STEERING_AND_CONTAMINATION_AUDIT

OTO:
    STATIC_OBSERVER_FOUNDATION_WITH_DYNAMIC_BRANCH_BLOCKED

CONVERGENCE:
    CAMPAIGN_PREREQUISITES_AND_NONCLAIMS_COLLAPSED

DIVERGENCE:
    OPTICAL_AND_ADVERSARIAL_NATIVE_TAXONOMIES_PRESERVED

CURRENT_DISPOSITION:
    HOLD_FOR_COMPOSITE_FIRST_TOUCH GATE

SCIENTIFIC_AUTHORITY:
    NONE
```

The campaign is not lost in breadth. It has reached the point where breadth can be organized: one constitutional spine, two heads, explicit collapse states, and a preserved divergence ledger. The next report or gate should consume this structure rather than reopen the blind split.

---

## 15. Evidence index

Primary artifacts reviewed:

- [Sol campaign report](sol-campaign/seal/SOL_CAMPAIGN_EXECUTION_REPORT.md)
- [Sol Part 2 report](sol-part2/seal/SOL_PART2_EXECUTION_REPORT.md)
- [Sol P2B report](sol-p2b/seal/SOL_P2B_EXECUTION_REPORT.md)
- [Kammi campaign report](kammi-campaign/seal/KAMMI_CAMPAIGN_EXECUTION_REPORT.md)
- [Kammi P2 report](kammi-p2/seal/KAMMI_P2_EXECUTION_REPORT.md)
- [OTO-A1 typed findings](authority-surface/seal/OTO_A1_TYPED_FINDINGS.json)
- [OTO-L typed findings](literature-adjacency/seal/OTO_L_TYPED_FINDINGS.json)
- [OTO-RT typed findings](runtime-transport/seal/OTO_RT_TYPED_FINDINGS.json)

This report is a cross-branch synthesis artifact. It adds no target, no object, no population, no observer intervention, no runtime authority, and no scientific finding.
