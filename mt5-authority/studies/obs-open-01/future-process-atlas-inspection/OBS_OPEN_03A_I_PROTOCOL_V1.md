# OBS-OPEN-03A-I — Descriptive Atlas Anatomy Supplement

Authority class: deterministic descriptive projection only.

Parent authority: `CAUSAL_FUTURE_PROCESS_ATLAS_V1`.

Parent root: `5f6ee8f323cbddfb91589e31b442c9972a23f1a4dd6146bc74b4667b98908eae`.

The supplement consumes only the sealed D_A atlas. It adds no outcome family, formal inference,
ranking, promotion, economic interpretation, or 03B target selection. D_B outcome reads,
applications, inspections, and scores remain zero. D_C remains unopened.

## Required anatomy

1. Candidate persistence survival and conditional hazard across physical age, separately under
   `ANCHOR_WEIGHTED` and `SESSION_WEIGHTED`, with counts and typed censoring.
2. Candidate persistence against exact retrospective final-session multiplicity and deterministic
   quartile projections. Final multiplicity is never represented as birth-time information.
3. Range terminal-location anatomy in `(k,h)` and `(k,t_eval)` coordinates, where
   `t_eval = 09:30 + k + h` and `t_freeze = 09:30 + k`.
4. Raw and range-normalized sibling geometry plus reconstructible width distributions.
5. A support/censoring twin for every emitted surface.
6. Raw and normalized path-variation anatomy, explicitly retaining its possible mechanical
   accumulation with elapsed observation time.
7. A question-generation ledger whose entries have authority `HYPOTHESIS_GENERATION_ONLY`.

## Identifiability boundaries

- Candidate age is the survival-process time axis for birth anchors. It is not a birth-time
  predictor. An age-conditioned future question requires a separately frozen causal landmark
  anchor protocol.
- Final session candidate multiplicity is retrospective. A later causal variable would need to be
  accumulated births or birth intensity available at the anchor time.
- In this observer generation, `k` and freeze clock are deterministic aliases. The supplement
  cannot identify a pure construction-scale relation independently of freeze clock.
- Raw and normalized views are peers. Normalization may create or amplify visible morphology.

## Stop rule

Seal the supplement and stop. Do not construct OBS-OPEN-03B-P. Do not read D_B or D_C outcomes.
