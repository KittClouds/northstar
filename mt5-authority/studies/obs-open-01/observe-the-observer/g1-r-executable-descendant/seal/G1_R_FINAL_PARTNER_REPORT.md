# G1-R Final Partner Report

## Shared outcome

G1-M is closed as archaeology. It established that historical executable chain-of-custody cannot be recovered from the surviving authority material.

G1-R therefore qualified a new descendant instead of attempting historical identity recovery.

```text
RESULT = G1_EXECUTABLE_DESCENDANT_QUALIFIED_WITH_RESTRICTIONS
MAXIMUM_AUTHORITY = G1_EXECUTABLE_DESCENDANT_V1
HISTORICAL_G1_BINARY_IDENTITY = NOT_CLAIMED
FC01A = HOLD_PENDING_NEW_INVOCATION-BINDING_ATTEMPT
POPULATION = UNOPENED
```

## To Sol — positive reconstruction binding

The new executable is a descendant of the exact sealed G1 source/semantic authority.

The semantic crate is consumed as a sealed path dependency. Its source manifest hash equals the sealed G1 root. The new wrapper contains no lifecycle, genealogy, rejection, temporal, or state-transition implementation. It only decodes synthetic inputs, invokes the exact G1 `init` and `step` boundary, captures `StepResult`, and serializes the result.

Two release builds were produced with the declared Rust 1.96.0 MSVC toolchain. Their executable bytes differ, so binary reproducibility is not claimed. Their complete semantic execution envelopes are byte-identical.

## To Kammi — adversarial identity attack

The static and synthetic attack battery found no failing witness within its declared scope.

It confirmed:

- no parallel semantic helper;
- no custom lifecycle or genealogy rule;
- no wrapper-side rejection reinterpretation;
- canonical input order is passed to G1 unchanged;
- sequence-gap rejection and unqualified-input rejection remain inherited G1 results;
- no population or environment-dependent scientific path was found.

Historical candidate binaries remain corroboration-only. Their callable interface was not treated as qualified, and agreement with them would not establish historical identity.

## To Kage — closure and authority boundary

The Sol binding receipt, Kammi attack receipt, synthetic replay outputs, and access audit form the G1-R closure envelope.

The qualified claim is deliberately narrow:

> A newly built executable callable has been qualified as a descendant of the sealed G1 source/semantic authority under the declared source, build, runtime, wrapper, and synthetic-input scope.

It does not authorize FC-01A, G8 transport, token realization, optical execution, population access, or any claim that the historical binary has been recovered.

## Qualification evidence

The frozen synthetic corpus contains three cases:

1. normal ordered transitions with strict upper/lower renewal and incomplete coverage;
2. sequence-gap rejection;
3. rejection of unqualified L2 input authority.

All three matched their predeclared terminal results in both builds. Semantic envelope root:

```text
D64BE09A306A66DD7490B4D2E965769598BCFBF09A9378863516A6D5D58CA675
```

## Frozen next state

```text
LIVE = new FC01A invocation-binding attempt against G1_EXECUTABLE_DESCENDANT_V1
FROZEN = historical A1 Attempt 1, FC01B+, G1-M archaeology
BLOCKED = population access, pair construction, token realization, P1-P5 execution
UNAUTHORIZED = G1-R2, semantic redesign, historical identity relabeling
```

No protected population content was opened.
