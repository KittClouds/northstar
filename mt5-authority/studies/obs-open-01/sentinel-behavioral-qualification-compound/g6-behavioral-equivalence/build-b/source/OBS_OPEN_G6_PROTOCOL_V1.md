# OBS-OPEN G6 — Behavioral Equivalence Contract

G6 freezes the meaning of protected causal-observer sameness before any pair search,
separator search, quotient construction, state merging, or minimization.

## Bound authority

- G3 root: `c936eac1c37cbc70aa9d42bffdea096cdc1afb9269ad73c26f2ddf535eb54bef`
- G4 root: `37ed98b4ebe447ef3c2152e550c99652d0157aea2c77b8a886379d1ba9e08e15`
- G5 root: `808f4089ada22e7736460a90efd71b1dec0726dece460ca4dc1df8cbccac554c`

The primitive compared objects remain qualified executable histories with context.
No state-sufficiency theorem is assumed.

## Global comparison architecture

`Theta*` contains the immutable G4 protected-observation authority, the fixed G5
relative-token coupling architecture, a deterministic affine price/time and
structural-genealogy correspondence architecture, the 23 named-observable matching
rules, finite-horizon authority, and inherited reachability authority.

Pair-specific coupling and correspondence instances are generated by this one
architecture. They are not pair-specific definitions of sameness.

The experimental token sequence is finite and open-loop. Token realization is
side-specific, causal, and non-anticipating. `Lambda` couples future questions;
`M` corresponds protected answers. `Lambda != M`.

## Comparison fibers

The contract is fiberwise. A fiber fixes semantic context shape: canonical price
unit, source/storage time resolution, cadence, session duration, range count, and
range geometry/freeze shape up to declared affine price/time origins.

Comparability additionally requires equality of the true unilateral neutral-token
continuation languages. This continuation-language consonance prevents an
epsilon-only intersection from hiding lawful one-sided future experiments.

Cross-fiber pairs are context-incompatible, not behaviorally mismatched.

## Behavioral relation

Within a comparison fiber, two qualified histories are semantically behaviorally
equivalent exactly when the G4 protected prefix projections match and every finite
token sequence in their true consonant G5 language produces transactionally
corresponding protected traces under `Theta*`.

Applied/rejected class and rejection reason are behavioral. Source, context, and
prefix presentation failures are comparability failures, not kernel behavior.
Experiment-token ordinal and applied causal ordinal remain distinct.

Candidate genealogy is compared structurally by a precommitted, transactional
alpha-correspondence. Numeric candidate IDs are not cross-history identity.
Committed correspondence cannot be backpatched after mismatch.

## Law authority

Reflexivity, symmetry, and transitivity are derived within each declared fiber from:

- identity/inverse/composition laws of affine context correspondence;
- identity/inverse/composition laws of genealogy bijections;
- common neutral-token refinement for coupling;
- continuation-language consonance;
- componentwise equivalence laws of all named-observable match rules.

Fixtures validate the implementation. They are not the law proofs.

## Inherited restrictions

The exact semantic definition is distinct from instance-certification authority.
G3/G5 upper-envelope uncertainty and G4's `GrammarStateAndEvent = NOT_EVALUABLE`
remain orthogonal restrictions. All-finite authority earns no omega-trace or
liveness authority.

## Firewalls and non-claims

Outcome reads for D_A, D_B, D_C, and D_D are zero. D_D accrual is untouched.

G6 does not search for pairs or witnesses and earns no claim that any distinct real
histories are equivalent or inequivalent. It constructs no quotient and earns no
state redundancy, removability, minimality, prediction, economic, or trading claim.

Any downstream change to coupling, correspondence, observable matching, domain,
vacuity, or horizon semantics requires a new G6 root and reexecution of G7+.

